# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FirebaseSandbox' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FirebaseSandbox
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'

  target 'FirebaseSandboxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FirebaseSandboxUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
